// supabase.js — simple initializer (no keys embedded)
export function initSupabase() {
  // Placeholder: in production, put keys in server or env
  // Example: import { createClient } from '@supabase/supabase-js'
  // const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
  // window.supabase = supabase;
  console.log("Supabase init placeholder. Add real init code here.");
}
