{
  "name": "genie-mobile-shell",
  "private": true,
  "version": "1.0.0",
  "description": "APK-ready shell scaffold for Genie using a mobile wrapper workflow.",
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/core": "latest"
  },
  "scripts": {
    "init:capacitor": "npx cap init Genie cloudora.genie.app --web-dir ../frontend",
    "sync:web": "npx cap sync android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "build:apk": "echo Build APK from Android Studio using Build > Build Bundle(s) / APK(s) > Build APK(s)"
  }
}
